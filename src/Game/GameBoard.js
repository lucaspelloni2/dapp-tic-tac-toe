import React, {Component} from 'react';
import styled from 'styled-components';

const Container = styled.div``;

class GameBoard extends Component {
  constructor() {
    super();
  }

  render() {
    return <Container>Game board component</Container>;
  }
}

export default GameBoard;
